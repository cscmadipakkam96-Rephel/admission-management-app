// Single place to edit the institute's own name/branch — the AI-generated
// student review (server/controllers/reviewController.js) always weaves
// this in naturally, the same way "CSC Pallavaram" appears in the
// reference example. Change this one value if it's wrong or the branch
// changes — nothing else needs editing.
const REVIEW_KEYWORDS = {
  companyName: "CSC Madipakkam",
};

module.exports = REVIEW_KEYWORDS;
